#from flask import Flask, jsonify
#import mysql.connector

#app = Flask(__name__)

#db = mysql.connector.connect(
    #host="soon",      
    #user="soon",
    #password="soon",
    #database="soon"
#)

#@app.route('/users')
#def get_users():
    #cursor = db.cursor(dictionary=True)
    #cursor.execute("SELECT * FROM users")  # or any table you have
    #rows = cursor.fetchall()
    #return jsonify(rows)

#if __name__ == '__main__':
    #app.run(debug=True)
